[build]
  command = "vite build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = []

  [functions.esbuild]
    loader = { ".js" = "jsx", ".mjs" = "jsx" }